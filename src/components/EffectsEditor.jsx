export default function EffectsEditor({ profile, setProfile }) { const set=(k,v)=>setProfile?.((p)=>({...p,[k]:v})); return <div className="editor-panel"><p className="eyebrow">effects</p><h2>Premium soft effects.</h2><label>Effect intensity<select value={profile.effect_intensity || 'Medium'} onChange={(e)=>set('effect_intensity', e.target.value)}><option>Off</option><option>Low</option><option>Medium</option><option>Premium</option></select></label><label className="switch-row"><input type="checkbox" checked={profile.show_custom_cursor !== false} onChange={(e)=>set('show_custom_cursor', e.target.checked)} /> Custom cursor</label></div> }
